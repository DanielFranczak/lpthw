formatter = "{} {} {} {} {}"
x = "It is really simple "
print(formatter.format(1, 2, 3, 4, 5))
print(formatter.format("one", "two", "three", "four", "five"))
print(formatter.format('True2', False, True, False, True))
print(formatter.format(formatter, formatter, formatter, formatter, x ))
print(formatter.format(
    "Life is not a pain",
    "Life is miracle",
    "We should celebrate it",
    "No matter how deep in ass we're",
    "So we need to tak it how it is"



))
